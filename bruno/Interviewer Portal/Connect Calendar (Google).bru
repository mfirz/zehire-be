meta {
  name: Connect Calendar (Google)
  type: http
  seq: 8
}

get {
  url: {{base_url}}/i/{{interviewer_token}}/connect/google
  body: none
  auth: none
}

docs {
  Initiate Google Calendar OAuth connection flow.

  Uses magic token in URL path for authentication.
  Redirects to Google OAuth consent screen.
}
